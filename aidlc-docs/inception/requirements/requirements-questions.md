# Requirements Analysis — 確認質問（Phase 1）

> 既決事項（公開/非公開デフォルト非公開、Phase1=絵＋メモ＋検索 等）は再確認しない。
> 以下は issue #21/#22 §8 で「未確定」とされた点。オーナー回答後に requirements.md を確定する。

## Q1. 振り返りメモのフィールド構成
A) 自由記述1本（Markdown）だけ
B) 構造化フィールド（上手くいかなかった点／試したこと／次の改善／モチーフ／フラグ）
C) 自由記述＋任意の構造化（自由記述必須、構造化は埋めたい時だけ）【推奨】
[Answer]:

## Q2. 自動タグ（日本語モチーフ）の公開可否
A) 公開（公開ギャラリーでタグ絞り込みを誰でも使える）
B) 非公開（管理モード時のみ表示・検索）
C) タグ単位で公開/非公開を選べる
[Answer]:

## Q3. 既存592枚のバックフィル範囲
A) 全部に「埋め込み＋日本語タグ」生成（一度きり ≈ $10）
B) 埋め込みのみ（≈ $0.5、タグ生成は後回し）
C) バックフィルしない（新規アップロード分だけ対象）
[Answer]:

## Q4. セマンティック検索の方式（方向性。詳細は ADR-002 で）
A) Bedrock Nova 多モーダル埋め込み（日本語クエリ可・少額）【推奨】
B) ブラウザ内 多言語CLIP（Transformers.js、無料・画像を外に出さないがモデルDL重い）
C) ここでは決めず ADR-002 で両案を比較してから裁定
[Answer]:
